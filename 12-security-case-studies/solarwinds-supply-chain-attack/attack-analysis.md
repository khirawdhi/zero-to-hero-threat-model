# Attack Analysis

Attackers gained access to the SolarWinds build environment.

They modified the build process to include a malicious backdoor in compiled software updates.

Because the updates were digitally signed, downstream customers trusted and installed the compromised software.

The malicious code then communicated with attacker-controlled infrastructure.
